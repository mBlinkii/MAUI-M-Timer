# --- Internal planning doc, not for the public repo ---
ARCHITECTURE.md

# --- Local-only manual release script ---
Build-Release.ps1
Build-Release.bat

# --- Vendored/external libraries ---
# Fetched fresh by the release workflow (.github/workflows/release.yml) and by
# Build-Release.ps1, so they are not tracked in version control
# (see MauiMPlusTimer/Libs/README.md).
MauiMPlusTimer/Libs/*
!MauiMPlusTimer/Libs/README.md

# --- Release build output ---
.release/
*.zip

# --- OS / editor cruft ---
.DS_Store
Thumbs.db
*.swp
*.tmp

# --- WoW client artifacts (in case the addon is symlinked/copied for testing) ---
WowLuaSettings.lua
